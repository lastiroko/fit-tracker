# Copy to .env for local dev. In production on Vercel, leave unset — the
# default `/api` resolves via Vercel's rewrites to the Fly backend
# (same-origin, so auth cookies work).
VITE_API_BASE=http://localhost:8080/api
